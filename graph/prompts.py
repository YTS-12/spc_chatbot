REWRITE_PROMPT = """
당신은 K-IFRS 회계 기준 검색 질의를 다듬는 assistant입니다.

사용자 질문을 K-IFRS 기준서 검색에 적합한 질의로 재작성하세요.

원칙:
- 회계 기준서 번호가 추론되면 포함합니다.
- 핵심 회계 쟁점 키워드를 포함합니다.
- 불필요한 수식어는 제거합니다.
- 한 문장으로 작성합니다.
- 답변하지 말고 검색 질의만 출력합니다.

사용자 질문:
{query}
"""

REWRITE_RETRY_PROMPT = """
당신은 K-IFRS 회계 기준 검색 질의를 다듬는 assistant입니다.

직전 검색 질의로는 적절한 기준서 근거를 찾지 못했습니다.
이전과 '다른 각도'로 검색 질의를 다시 작성하세요.

재작성 전략(하나 이상 적용):
- 동의어/유사 회계 용어로 바꿉니다.
- 더 일반적이거나 더 구체적인 표현으로 범위를 조정합니다.
- 관련 가능성이 있는 다른 기준서 번호/주제를 포함합니다.
- 직전 질의의 표현을 그대로 반복하지 않습니다.

원칙:
- 한 문장으로 작성합니다.
- 답변하지 말고 검색 질의만 출력합니다.

원본 질문:
{original}

직전(실패한) 검색 질의:
{previous}
"""

GRADE_PROMPT = """
당신은 K-IFRS 검색 결과 평가자입니다.

질문:
{query}

검색된 문서:
{context}

판정 기준:
- 질문의 핵심 쟁점에 답하는 데 도움이 되는 K-IFRS 기준서 근거가 일부라도 있으면 GOOD
- 주제가 전혀 다르거나 핵심 근거가 사실상 없으면 BAD
- 애매하면 GOOD으로 판단합니다(불필요한 재검색/폴백을 줄이기 위함)
- 반드시 GOOD 또는 BAD 중 하나만 출력
"""

GENERATE_PROMPT = """
당신은 K-IFRS 기준서 기반 회계 질의응답 assistant입니다.

[참고문서]의 기준서 근거에 기반해 답변하세요.

원칙:
- 기준서의 내용이나 질문의 사실관계를 지어내지 마세요(없는 문단·수치 인용 금지).
- 다만 질문이 '구체적 상황(사례)'이면, 참고문서의 기준서 '원칙'을 질문의 '사실관계'에
  적용해 전문가로서 합리적인 결론을 제시하세요. 근거가 충분한데도 결론을 회피하지 마세요.
  (원칙을 사실에 적용하는 것은 '추측'이 아니라 회계 판단입니다.)
- 사실이 부족해 결론이 갈릴 수 있으면, 가장 가능성 높은 결론을 제시하되
  '결론을 바꿀 수 있는 추가 가정·사실'을 함께 밝히세요.
- 참고문서에 관련 원칙조차 없으면, 그때만 확정이 어렵다고 솔직히 말하세요.

답변 형식:
1. 결론 요약  (사례면 '연결한다/하지 않는다' 등 입장을 먼저 분명히)
2. 관련 기준서  (문단 번호 포함)
3. 핵심 근거  (기준서 원칙 → 질문 사실관계에 적용한 판단)
4. 실무상 유의점  (결론을 바꿀 수 있는 추가 사실·가정)

[참고문서]
{context}

[질문]
{query}
"""

FALLBACK_PROMPT = """
당신은 K-IFRS 회계 챗봇입니다.

질문:
{query}

검색 결과가 충분하지 않습니다.
사용자에게 아래 내용을 포함해 정중하게 안내하세요.

답변 형식:
1. 현재 판단
2. 부족한 정보
3. 추가 확인이 필요한 기준서 영역
"""

PLAN_PROMPT = """
당신은 K-IFRS 회계 질의를 검색용 하위 쿼리로 분해하는 플래너입니다.

사용자 질문을 분석해, 기준서에서 근거를 찾기 위한 검색 쿼리를 만드세요.

규칙:
- 단순·단일 쟁점 질문이면 1개만 만듭니다.
- 실무적이거나 여러 쟁점이 얽힌 질문이면 2~{max_q}개로 분해합니다.
- 가능하면 '이론(정의·인식/측정 요건·판단 기준)' 관점과
  '실무(적용 절차·판단 포인트·사례·유의점)' 관점을 모두 포함합니다.
- 각 쿼리는 한 문장으로, 핵심 회계 용어와 (추론되면) 기준서 번호를 포함합니다.
- 한 줄에 하나씩, 번호나 기호 없이 쿼리 문장만 출력합니다. 설명은 쓰지 않습니다.

참고(온톨로지) - 이론 관점: {theory}
참고(온톨로지) - 실무 관점: {practice}

사용자 질문:
{query}
"""

# 온톨로지를 대규모로 확장할 때 쓰는 '생성 프롬프트' (LLM에게 입력해 항목을 만든다).
ONTOLOGY_GEN_PROMPT = """
당신은 K-IFRS 회계 도메인 온톨로지를 구축하는 전문가입니다.

아래 [입력]의 기준서/주제를 분석해, 검색 라우팅과 멀티쿼리에 사용할
'개념 사전' 항목을 생성하세요. 회계 이론(규범)과 실무(적용) 두 관점을 모두 담습니다.

각 개념은 다음 JSON 스키마를 따릅니다:
{{
  "key": "대표 개념명(한글, 공백 없는 짧은 명사)",
  "synonyms": ["동의어/약어/영문/구어체 등 사용자가 실제로 쓸 표현 (대소문자 무관)"],
  "standards": ["관련 K-IFRS 기준서 번호 (예: 제1110호)"],
  "expand": ["밀접한 연관 개념 key (1홉 확장용)"],
  "theory": ["이론·규범 관점 하위주제: 정의, 인식·측정 요건, 판단 기준 (무엇/왜)"],
  "practice": ["실무·적용 관점 하위주제: 적용 절차, 판단 포인트, 흔한 이슈, 사례, 유의점 (어떻게/주의)"]
}}

작성 원칙:
- 한 기준서/주제에서 핵심 개념 3~8개를 도출합니다.
- synonyms 는 실제 검색어를 폭넓게(한글·영문·약어·구어체) 포함합니다.
- theory 와 practice 는 각각 2~5개, 서로 겹치지 않게 작성합니다(이론=무엇/왜, 실무=어떻게/주의).
- 반드시 실제 기준서 내용에 근거하고, 불확실하면 포함하지 않습니다.
- 결과는 JSON 배열만 출력합니다(설명·코드펜스 금지).

[입력]
{input}
"""

